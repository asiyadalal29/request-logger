pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t asiyadalal29/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag asiyadalal29/request-logger:${env.BUILD_ID} asiyadalal29/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push asiyadalal29/request-logger:${env.BUILD_ID}"
        sh "docker push asiyadalal29/request-logger:latest"
      }
    }
  }
}
